<!doctype html>
<html lang="en">

    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{title}}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

        <style>
            :root {
                --timeline-color: rgba(0, 0, 0, 0.4);
                --timeline-size: 2px;
                --totty: 42, 40, 94;
            }

            .btn-primary {
                --bs-btn-bg: rgb(var(--totty));
                --bs-btn-border-color: rgb(var(--totty));
                --bs-btn-hover-bg: rgb(var(--totty));
                --bs-btn-hover-border-color: rgb(var(--totty));
                --bs-btn-active-bg: rgb(var(--totty));
                --bs-btn-active-border-color: rgb(var(--totty));
            }

            .nav-pills .nav-link {
                color: #FFF;
            }

            .nav-pills .nav-link.disabled {
                color: #CCC;
            }

            .nav-pills .nav-link.active,
            .nav-pills .show>.nav-link,
            .nav-pills .nav-link:hover {
                background-color: #fff;
                color: rgb(var(--totty));
            }

            .btn:active {
                position: relative;
                top: 1px;
            }

            .timeline::after {
                content: "";
                border-left: solid var(--timeline-size) var(--timeline-color);
                display: block;
                margin-left: auto;
                margin-right: auto;
                height: 100%;
                width: 2px;
            }

            .timeline-header {
                overflow: hidden;
                text-align: center;
            }

            .timeline-header:before,
            .timeline-header:after {
                background-color: var(--timeline-color);
                content: "";
                display: inline-block;
                height: var(--timeline-size);
                position: relative;
                vertical-align: middle;
                width: 50%;
            }

            .timeline-header:before {
                right: 0.5em;
                margin-left: -50%;
            }

            .timeline-header:after {
                left: 0.5em;
                margin-right: -50%;
            }

            .timeline .away{
                text-align: right;
                direction: rtl;
            }

            body {
                padding-bottom: 3rem;
            }

            .outcome-col {
                position: relative;
                max-height: 200px;
            }

            .outcome-col canvas {
                margin-left: auto;
                margin-right: auto;
            }

            #back {
                width: 3rem;
                height: 3rem;
                float: left;
                margin-left: 2rem;
                margin-top: 1rem;
            }

            header {
                vertical-align: middle;
                background-color: rgb(var(--totty));
                color: #FFF;
            }

            .outcomes-home {
                background-color: rgba(var(--home), 0.4);
            }

            .outcomes-away {
                background-color: rgba(var(--away), 0.4);
            }

            h2 {
                text-decoration: underline;
            }

            .icons svg {
                opacity: 0.6;
                transform: skew(-14deg);
            }

            .stat,
            .substat,
            .subsubstat {
                color: #FFF;
                background-color: rgba(var(--totty), var(--opacity, 1));
            }

            .substat {
                --opacity: 0.8;
            }

            .subsubstat {
                --opacity: 0.6;
            }

            .subsubstat div {
                padding-left: 2rem;
                font-style: italic;
            }

            * {
                hyphens: auto;
                -webkit-hyphens: auto;
                overflow-wrap: break-word;
                word-break: normal;
            }
        </style>
    </head>

    <body>
        {{{body}}}
    </body>

</html>