<!doctype html>
<html lang="en">

    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{title}}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

        <style>
            :root {
                --timeline-color: rgba(0, 0, 0, 0.4);
                --timeline-size: 2px;
                --totty: #2A285E;
            }

            .btn-primary {
                --bs-btn-bg: var(--totty);
                --bs-btn-border-color: var(--totty);
                --bs-btn-hover-bg: var(--totty);
                --bs-btn-hover-border-color: var(--totty);
                --bs-btn-active-bg: var(--totty);
                --bs-btn-active-border-color: var(--totty);
            }

            .nav-pills .nav-link.active, .nav-pills .show>.nav-link {
                background-color: var(--totty);
            }

            .btn:active{
                position: relative;
                top: 1px;
            }

            .timeline::after {
                content: "";
                border-left: solid var(--timeline-size) var(--timeline-color);
                display: block;
                margin-left: auto;
                margin-right: auto;
                height: 100%;
                width: 2px;
            }

            .timeline-header {
                overflow: hidden;
                text-align: center;
            }

            .timeline-header:before,
            .timeline-header:after {
                background-color: var(--timeline-color);
                content: "";
                display: inline-block;
                height: var(--timeline-size);
                position: relative;
                vertical-align: middle;
                width: 50%;
            }

            .timeline-header:before {
                right: 0.5em;
                margin-left: -50%;
            }

            .timeline-header:after {
                left: 0.5em;
                margin-right: -50%;
            }

            body{
                padding-bottom: 3rem;
            }
        </style>
    </head>

    <body>
        <div class="container">
            {{{body}}}
        </div>
    </body>

</html>